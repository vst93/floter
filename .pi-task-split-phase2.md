# App.tsx 第二阶段拆分 + App.css 按表面拆文件

## 背景

App.tsx 仍 2494 行，App.css 4142 行。按已存在的边界继续拆。

## 任务 1：App.tsx 拆 3 个 hook

按已存在的边界拆三个自定义 hook：

1. **`useAppKeyboard(...)`** — 窗口级 onKeyDown（App.tsx:976-1370，~390 行）：terminal/settings/plugin/collapsed 四个模式的按键分发
2. **`useLauncherActions(...)`** — 启动器执行簇：runCommand/resumeTerminalSession/launchApplication/openWithSystem/runSystemAction/runLauncherItem/handleLauncherKey（App.tsx:1624-1980，~350 行）
3. **`useTimedFeedback(...)`** — showLauncherFeedback/showTerminalFeedback（App.tsx:350-372）是逐字重复的计时器对

## 任务 2：App.css 拆文件

拆为：
- `src/styles/base.css`（变量/滚动条/reduced-motion）
- `launcher.css`
- `terminal.css`
- `settings.css`
- `extensions.css`
- `pinned-card.css`

App.tsx 或 main.tsx 按序 import。选择器原样搬移，零视觉变化。

## 约束

- **只移动代码，不改行为**
- 每个 hook 单独一个文件，放在 `src/hooks/` 下
- CSS 选择器原样搬移

## 验证

1. `npx tsc --noEmit` 通过
2. `npm run build` 通过
