# SessionsPage 键盘无障碍

## 问题

resumable 行虽然可点击，但缺少键盘支持：无 role/tabIndex，↑/↓ 导航（f1b14da）到这里没有对应的 Enter/Space 激活，focus-visible 样式也缺失。

## 方案

resumable 行加 `role="button"`、`tabIndex={0}`、`aria-disabled`（exited/attached 行），`onKeyDown` 处理 Enter/Space 调 `onResume`；App.css 为 `.session-manager__row--resumable:focus-visible` 复用 hover 的 wash + 一圈 `--accent` 内描边。零逻辑变更。

## 参考文件

- `src/settings/SessionsPage.tsx` — ~8 行
- `src/App.css` — ~10 行

## 约束

- 只加 a11y，不改功能逻辑

## 验证

1. `npx tsc --noEmit` 通过
2. `npm run build` 通过
