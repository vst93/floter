# 下一步迭代建议

## 背景

已完成三轮迭代：
1. 集成页面布局优化（10 项）
2. 启动器动画 + 空状态 + sessions kill 确认 + App.tsx 拆分 + source 换行 + Wayland 修复
3. 终端 header 状态 + Sessions 行交互 + 设置页导航 + App.tsx 拆 hooks（本轮）

现在需要 pi 审视整个 floter 项目，给出下一轮最值得做的迭代方向。

## 任务

审查当前代码，给出 3-5 个最值得做的下一步迭代建议。按优先级排列，每个建议包含：
- 问题描述（引用文件/行号）
- 方案概述
- 影响范围
- 优先级（高/中/低）

## 约束

- 聚焦布局/交互打磨，零功能逻辑变更
- 延续之前的迭代风格
